% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun_pivot_cmc.R
\name{pivot_cmc}
\alias{pivot_cmc}
\title{Pivot CMC Weekly Sheets to Long Format}
\usage{
pivot_cmc(df)
}
\arguments{
\item{df}{Raw imported CMC weekly sheet.}
}
\value{
Long-format tibble with trap_name, date_trap_set, Zone,
mosquito_species, mosquito_count, Week, and other pass-through columns.
}
\description{
CMC 2015-2018 weekly files are wide: one column per species
(\verb{Cx tarsalis}, \verb{Cx pipiens}). The downstream chain expects one row per
(trap, species) with \code{mosquito_species} and \code{mosquito_count} columns.
}
\details{
This function:
\enumerate{
\item Renames \verb{Trap Number} -> \code{trap_name} and \verb{Trap Date} -> \code{date_trap_set}
so key_rename() maps them to the canonical names.
\item Pivots the two species columns to long format.
\item Synthesizes a "malfunction" species row for any trap-date where
\code{Malfunction} is not "NO" so wnv_s_clean()'s trap_status logic fires.
}

Column casing is inconsistent across years; the function normalises
multi-whitespace before matching.
}
