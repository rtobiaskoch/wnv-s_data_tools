% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun_expand_trap.R
\name{expand_trap}
\alias{expand_trap}
\title{Build Full Trap × Year × Week × Species Skeleton}
\usage{
expand_trap(
  trap_ref,
  spp_levels,
  year_start = 2006L,
  year_end = lubridate::year(Sys.Date()),
  week_start = 23L,
  week_end = 37L,
  fc_zones = c("NE", "NW", "SE", "SW")
)
}
\arguments{
\item{trap_ref}{Data frame from foco_trap - data.csv.
Required columns: trap_id, zone, active (integer 0/1), start (integer year).}

\item{spp_levels}{Character vector of species from cfg$spp_levels.}

\item{year_start}{Integer floor year (from cfg$year_start).}

\item{year_end}{Integer ceiling year (default: current year).}

\item{week_start}{Integer first ISO week (from cfg$week_start).}

\item{week_end}{Integer last ISO week (from cfg$week_end).}

\item{fc_zones}{Character vector of zone codes that collapse to "FC" in zone2.
Defaults to c("NE", "NW", "SE", "SW"). Pass cfg$fc_zone at the call site.}
}
\value{
Data frame: key, trap_id, zone, zone2, method, year, week, spp.
}
\description{
Creates the rectangular grid into which all observed data is joined. This
function produces the structure only — no cleaning, no status assignment.
}
\details{
Boulder County (BC) traps are included regardless of active status because
their pool submissions are inconsistent; excluding inactive BC traps drops
legitimate surveillance records.

zone2 and method are fixed attributes per trap (not expanded):
zone2  = "FC" for NE/NW/SE/SW, else zone.
method = "G" if trap_id contains "gr", else "L".

Per-trap start years come from the trap_ref \code{start} column so that traps
deployed mid-surveillance do not generate rows before they existed.
year_start is a floor that filters out any year below it.
}
