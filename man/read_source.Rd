% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun_read_source.R
\name{read_source}
\alias{read_source}
\title{Read All Files Under a Source Root}
\usage{
read_source(
  path,
  pattern,
  pivot_fn = NULL,
  exclude_paths = character(),
  import_args = list()
)
}
\arguments{
\item{path}{Character. Directory to search recursively.}

\item{pattern}{Character. Regex passed to list.files().}

\item{pivot_fn}{Function or NULL. Applied to each file's imported df
BEFORE binding. Use to reshape wide formats to long.}

\item{exclude_paths}{Character vector. File paths containing any of these
substrings are dropped (e.g. "all_mosq").}

\item{import_args}{Named list. Extra args forwarded to rio::import()
(e.g. list(sheet = "CombinedData") for multi-sheet xlsx).}
}
\value{
A tibble of all rows from all matched files, all columns coerced to
character for consistent downstream handling.
}
\description{
Lists files recursively under \code{path} matching \code{pattern}, optionally drops
paths containing any string in \code{exclude_paths}, reads each with rio::import
(passing \code{import_args} for sheet selection etc.), applies an optional
per-file pivot function, then row-binds.
}
\details{
Replaces read_list() in the multi-source pipeline. Each invocation handles
exactly ONE source (one folder root, one pattern, one pivot rule).
}
