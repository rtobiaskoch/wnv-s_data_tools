% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun_fill_skeleton.R
\name{collect_unmerged}
\alias{collect_unmerged}
\title{Collect Unmerged Records from Each Source}
\usage{
collect_unmerged(skeleton, sources)
}
\arguments{
\item{skeleton}{Data frame from expand_trap(). Must have a \code{key} column.}

\item{sources}{Named list of data frames (same as passed to fill_skeleton()).
Each must contain a \code{key} column.}
}
\value{
Data frame of unmerged records with a \code{source} column added.
}
\description{
For each source in the priority list, returns records whose key is absent
from the skeleton. These flag traps that were observed but are not in the
active trap reference list. The output is a single data frame with a
\code{source} column identifying the originating data source.
}
