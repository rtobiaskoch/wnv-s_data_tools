<!-- generated by /context-docs on 2026-05-28 -->
# Function Inventory

27 functions across 27 files in `R/`. Tested = has corresponding `tests/testthat/test-*.R` case.

---

## Ingestion

### [R/fun_read_source.R](../../R/fun_read_source.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`read_source()`](../../R/fun_read_source.R#L24) | `read_source(path, pattern, pivot_fn=NULL, exclude_paths=character(), import_args=list())` | Lists files under `path` matching `pattern`, optionally drops paths containing `exclude_paths` substrings, reads each with `rio::import`, dispatches `pivot_fn`, binds rows | ✓ |

### [R/fun_read_list.R](../../R/fun_read_list.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`read_list()`](../../R/fun_read_list.R#L13) | `read_list(path, pattern)` | Reads all files matching pattern, coerces all columns to character, binds rows | — |

### [R/fun_pivot_cmc.R](../../R/fun_pivot_cmc.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`pivot_cmc()`](../../R/fun_pivot_cmc.R#L27) | `pivot_cmc(df)` | Pivots CMC wide format (Tarsalis/Pipiens columns) to long; synthesises malfunction rows where total is NA | ✓ |

### [R/fun_pivot_cfc.R](../../R/fun_pivot_cfc.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`pivot_cfc()`](../../R/fun_pivot_cfc.R#L20) | `pivot_cfc(df)` | Renames CFC-specific column names to the VDCI intermediate standard | ✓ |

### [R/fun_pivot_boulder.R](../../R/fun_pivot_boulder.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`pivot_boulder()`](../../R/fun_pivot_boulder.R#L28) | `pivot_boulder(df)` | Pass-through with zone enforcement — forces zone = "BC" on all rows | ✓ |

---

## Cleaning

### [R/key_rename.R](../../R/key_rename.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`key_rename()`](../../R/key_rename.R#L4) | `key_rename(df, rename_df, drop_extra=TRUE)` | Renames columns via lookup table from `database_column_rename.csv`; optionally drops unmapped columns | — |

### [R/fun_wnv_s_clean.R](../../R/fun_wnv_s_clean.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`clean_summary()`](../../R/fun_wnv_s_clean.R#L20) | `clean_summary(df0, df, col_name, label)` | Reports per-column cleaning outcome (added / no-op / transformed / cleaned) | — |
| [`wnv_s_clean()`](../../R/fun_wnv_s_clean.R#L96) | `wnv_s_clean(df, all_cols=FALSE, ...)` | Canonical cleaning chain: trim whitespace, parse dates, classify spp/method/trap_status, derive zone2/year/week | ✓ |

### [R/fun_make_key.R](../../R/fun_make_key.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`make_key()`](../../R/fun_make_key.R#L39) | `make_key(df, key_cols, name="key", sep="\|")` | Concatenates specified columns into a composite pipe-separated key column used for deduplication | — |

### [R/fun_dedup_culex.R](../../R/fun_dedup_culex.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`culex_dedup()`](../../R/fun_dedup_culex.R#L26) | `culex_dedup(df, key_cols)` | Removes weekend collection duplicates by grouping on `key_cols` and keeping the row with the maximum total | — |

### [R/fun_parse_flexible_date.R](../../R/fun_parse_flexible_date.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`parse_flexible_date()`](../../R/fun_parse_flexible_date.R#L1) | `parse_flexible_date(date_str)` | Tries 16 date format strings in sequence to parse non-standard date inputs from source files | — |

### [R/fun_fix_zone.R](../../R/fun_fix_zone.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`fix_zone_from_ref()`](../../R/fun_fix_zone.R#L10) | `fix_zone_from_ref(df, trap_ref)` | Overwrites the `zone` column using the authoritative `foco_trap` reference | — |

### [R/fun_rm_grp_NA.R](../../R/fun_rm_grp_NA.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`rm_grp_na()`](../../R/fun_rm_grp_NA.R#L12) | `rm_grp_na(df, group_vars, target_col)` | Removes groups where all values in `target_col` are NA | — |

---

## Structuring

### [R/fun_expand_trap.R](../../R/fun_expand_trap.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`expand_trap()`](../../R/fun_expand_trap.R#L60) | `expand_trap(trap_ref, spp_levels, year_start, year_end, week_start, week_end, fc_zones)` | Builds the full rectangular skeleton: every (trap_id × year × week × spp) cell that should exist | ✓ |

### [R/fun_prep_for_skeleton.R](../../R/fun_prep_for_skeleton.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`prep_for_skeleton()`](../../R/fun_prep_for_skeleton.R#L15) | `prep_for_skeleton(df, spp_levels)` | Expands cleaned data so every observed trap-week has one row per spp_level; status rows (malfunction, no culex, no mosq) are broadcast across all species | ✓ |

### [R/fun_row_wise_expand.R](../../R/fun_row_wise_expand.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`rowwise_expand()`](../../R/fun_row_wise_expand.R#L5) | `rowwise_expand(df, start, end, new_name)` | Expands rows based on a numeric start/end range column into one row per integer value | — |

---

## Filtering

### [R/fun_filter_culex_sheet.R](../../R/fun_filter_culex_sheet.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`filter_culex_sheet()`](../../R/fun_filter_culex_sheet.R#L23) | `filter_culex_sheet(df, na_col, trap_keep_df)` | Drops NA/blank `na_col` rows, distinct-deduplicates, then semi_joins to active trap list | — |

---

## Joining

### [R/fun_fill_skeleton.R](../../R/fun_fill_skeleton.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`fill_skeleton()`](../../R/fun_fill_skeleton.R#L28) | `fill_skeleton(skeleton, sources)` | Priority-joins a named list of data sources into the skeleton; once a (trap × week × spp) cell is filled, lower-priority sources cannot overwrite it | — |
| [`collect_unmerged()`](../../R/fun_fill_skeleton.R#L89) | `collect_unmerged(skeleton, sources)` | Anti-joins each source against the filled skeleton to capture rows that failed to match any skeleton cell | — |

### [R/fun_join_culex_sheets.R](../../R/fun_join_culex_sheets.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`join_cxsheet_database()`](../../R/fun_join_culex_sheets.R#L19) | `join_cxsheet_database(df_z1, df_z2, key_cols)` | Full-joins two sources on (trap_id, spp, year, week) and coalesces shared columns for QC comparison | — |

### [R/fun_bind_culex_sheets.R](../../R/fun_bind_culex_sheets.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`bind_culex_sheets()`](../../R/fun_bind_culex_sheets.R#L1) | `bind_culex_sheets(culex, database)` | Binds two culex-format tibbles with a source label column | — |

### [R/fun_reformat_database_2_culex_sheet.R](../../R/fun_reformat_database_2_culex_sheet.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`reformat_database_2_culex_sheet()`](../../R/fun_reformat_database_2_culex_sheet.R#L1) | `reformat_database_2_culex_sheet(df)` | Aggregates pool database rows on (trap_id, trap_date, spp, method) to produce one row per trap-date | — |

---

## Validation

### [R/fun_assign_trap_status.R](../../R/fun_assign_trap_status.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`assign_trap_status_post_expand()`](../../R/fun_assign_trap_status.R#L24) | `assign_trap_status_post_expand(df)` | Resolves NA `trap_status` on skeleton rows that received data from the priority join, by inspecting zone-week data presence | ✓ |

---

## Manifest

### [R/fun_manifest.R](../../R/fun_manifest.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`manifest_init()`](../../R/fun_manifest.R#L10) | `manifest_init(path)` | Truncates and re-writes the manifest header; called once at pipeline start | ✓ |
| [`manifest_log()`](../../R/fun_manifest.R#L33) | `manifest_log(path, label, ...)` | Appends a named diagnostic block to the manifest; named `...` args become indented key: value lines | ✓ |
| [`inventory_source()`](../../R/fun_manifest.R#L56) | `inventory_source(path, pattern)` | Recursively counts files matching `pattern` under `path`; returns `n_files` and `files` vector | ✓ |

---

## Plotting

### [R/fun_plot_all_spp.R](../../R/fun_plot_all_spp.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`plot_all_spp()`](../../R/fun_plot_all_spp.R#L1) | `plot_all_spp(df, z)` | Bar chart of all observed species counts by zone/year/week — diagnostic for cleaning step | — |

### [R/fun_plot_bind_culex.R](../../R/fun_plot_bind_culex.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`plot_culex_datasheet_comp()`](../../R/fun_plot_bind_culex.R#L1) | `plot_culex_datasheet_comp(df, y)` | Point/line comparison plot of trap counts (culex sheet) vs pool counts (database) over time | — |

### [R/fun_plot_n.R](../../R/fun_plot_n.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`plot_n()`](../../R/fun_plot_n.R#L18) | `plot_n(df, ...)` | Generic tidy-eval group-count bar chart | — |

### [R/fun_plot_n_trap.R](../../R/fun_plot_n_trap.R)

| Function | Signature | Purpose | Tested? |
|----------|-----------|---------|---------|
| [`plot_n_trap()`](../../R/fun_plot_n_trap.R#L1) | `plot_n_trap(df, rm_zone)` | Stacked bar chart of trap counts by `trap_status` and zone — final QC visualisation | — |

---

## Test Coverage Summary

- **Tested (✓):** `read_source`, `pivot_cmc`, `pivot_cfc`, `pivot_boulder`, `wnv_s_clean`, `expand_trap`, `prep_for_skeleton`, `assign_trap_status_post_expand`, `manifest_init`, `manifest_log`, `inventory_source` — **11 functions, 34 test cases**
- **Not tested (—):** `read_list`, `key_rename`, `clean_summary`, `make_key`, `culex_dedup`, `parse_flexible_date`, `fix_zone_from_ref`, `rm_grp_na`, `rowwise_expand`, `filter_culex_sheet`, `fill_skeleton`, `collect_unmerged`, `join_cxsheet_database`, `bind_culex_sheets`, `reformat_database_2_culex_sheet`, `plot_*` — **16 functions**
- Highest-priority gaps: `fill_skeleton`, `filter_culex_sheet`, `culex_dedup` (all critical path, no tests)
